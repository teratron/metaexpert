/*struct Config {
    pub api_key: String,
    pub api_secret: String,
}

impl Config {
    pub fn new(api_key: String, api_secret: String) -> Config {
        Config {
            api_key,
            api_secret,
        }
    }
}*/
//use std::env;

//use dotenv_vault::dotenv;

//dotenv().expect(".env file not found");

//pub(crate) const BINANCE_API_KEY: String = env::var("BINANCE_API_KEY").unwrap_or("".to_string());
//pub(crate) const BINANCE_API_SECRET: String = env::var("BINANCE_API_SECRET").unwrap_or("".to_string());
