[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "Trading expert built with MetaExpert"
requires-python = ">=3.12"
dependencies = [
    "metaexpert>=0.3.0",
    "python-dotenv>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
